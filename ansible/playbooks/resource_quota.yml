apiVersion: v1
kind: ResourceQuota
metadata:
    name: webgoat-quota
    namespace: webgoat
spec:
    hard:
        requests.cpu: "2"
        requests.memory: "2Gi"
        limits.cpu: "4"
        limits.memory: "4Gi"
        pods: "10"
